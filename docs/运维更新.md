## 更新与打包

# 地图工具（生成 exe）
pyinstaller --onefile --windowed --icon=assets/铁路查询.ico src/pymap.py
# 或使用 spec：
pyinstaller specs/pymap.spec

# 用户端（Electron）
cd electron
npm install
npm start
npm run package

## @作者 半个才子 2025.8.4
